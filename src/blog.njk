{% extends "base.njk" %}
{% block content %} 
    <main>
        {% for post in collections.Blog %}
            <ul>
                <li>
                    Issue. {{post.data.number}} - <a href="{{post.url}}">{{post.data.title}}</a>
                </li>
            </ul>
        {% endfor %}
    </main>
{% block more %}{% endblock %}
{% endblock %}